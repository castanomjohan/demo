apiVersion: v1
kind: Service
metadata:
  name: opensearch-dashboards
spec:
  type: ClusterIP
  selector:
    app: opensearch-dashboards
  ports:
    - name: dashboards
      port: 5601
      targetPort: 5601
